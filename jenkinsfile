pipeline{
    stages{
        stage ('Git clone' ){
           steps{
                 
              sh 'git clone https://github.com/kgovinda1995/jenkins.git'
            }
         }
        stage ('Maven build') {
            steps {
            sh 'mvn clean install'
            }
            
        }       
    }
}
