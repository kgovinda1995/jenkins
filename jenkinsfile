pipeline{
    stages{
        stage ('Git clone' ){
           sh "git clone https://github.com/kgovinda1995/jenkins.git"

        }
        stage ('Maven build') {
            sh 'mvn clean install'
        }       
    }
}
